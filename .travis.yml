language: cpp
compiler: gcc

before_script:
    - cd ..
    - git clone https://github.com/livingcreative/kcommon.git kcommon
    - mkdir build && cd build
    - cmake ../kcanvas/src --build

script: make